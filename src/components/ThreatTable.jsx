import { ChevronRight, Search } from "lucide-react";
export default function ThreatTable({data,onSelect,search,setSearch,severity,setSeverity}) {
 return <div className="panel"><div className="panel-head"><div><h2>Threat Detection</h2><p>Latest ML predictions and security indicators</p></div><div className="filters"><label><Search size={16}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search event, type, user..."/></label><select value={severity} onChange={e=>setSeverity(e.target.value)}><option value="">All severity</option><option>Normal</option><option>Low Threat</option><option>Medium Threat</option><option>High Threat</option><option>Critical Threat</option></select></div></div><div className="table-wrap"><table><thead><tr><th>Event ID</th><th>Event Type</th><th>Prediction</th><th>Confidence</th><th>Severity</th><th>Timestamp</th><th></th></tr></thead><tbody>{data.map(x=><tr key={x.event_id} onClick={()=>onSelect(x.event_id)}><td className="mono">{x.event_id}</td><td>{x.event?.event_type||"—"}</td><td><span className={`pill ${x.prediction.toLowerCase()}`}>{x.prediction}</span></td><td><b>{x.confidence_score}%</b></td><td><span className={`severity ${x.severity.replaceAll(" ","-").toLowerCase()}`}>{x.severity}</span></td><td>{new Date(x.prediction_timestamp).toLocaleString()}</td><td><ChevronRight size={17}/></td></tr>)}</tbody></table>{!data.length&&<div className="empty">No matching predictions.</div>}</div></div>
}
